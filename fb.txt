{
  "version": "0.2.0",
  "configurations": [
    // --- ASP.NET Core backend ---
    {
      "name": "Backend - ASP.NET Core",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build-backend",
      "program": "${workspaceFolder}/backend/bin/Debug/net8.0/backend.dll",
      "args": [],
      "cwd": "${workspaceFolder}/backend",
      "stopAtEntry": false,
      "serverReadyAction": {
        // When backend prints "Now listening on: http://localhost:xxxx"
        "action": "openExternally",
        "pattern": "\\bNow listening on:\\s+(https?://\\S+)"
      },
      "env": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      },
      "sourceFileMap": {
        "/Views": "${workspaceFolder}/backend/Views"
      }
    },

    // --- React frontend ---
    {
      "name": "Frontend - React App",
      "type": "pwa-chrome",
      "request": "launch",
      "preLaunchTask": "npm: start frontend",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/frontend"
    }
  ],

  "compounds": [
    {
      "name": "Launch Both (React + ASP.NET Core)",
      "configurations": [
        "Backend - ASP.NET Core",
        "Frontend - React App"
      ],
      "stopAll": true
    }
  ]
}



{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-backend",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "${workspaceFolder}/backend"],
      "problemMatcher": "$msCompile"
    },
    {
      "label": "npm: start frontend",
      "type": "npm",
      "script": "start",
      "path": "frontend"
    }
  ]
}